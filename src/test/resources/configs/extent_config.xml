<?xml version="1.0" encoding="UTF-8"?>
<extentreports>
  <configuration>

    <!-- report theme -->
    <!-- STANDARD, DARK -->
    <theme>DARK</theme>

    <!-- document encoding -->
    <!-- defaults to UTF-8 -->
    <encoding>UTF-8</encoding>

    <!-- protocol for script and stylesheets -->
    <!-- defaults to https -->
    <!-- HTTP, HTTPS -->
    <protocol>HTTPS</protocol>

    <!-- offline report -->
    <timelineEnabled>true</timelineEnabled>
    
    <!-- offline report -->
    <enableOfflineMode>false</enableOfflineMode>
    
    <!-- use thumbnails for base64 images -->
    <!-- this may slowdown viewing tests -->
    <thumbnailForBase64>false</thumbnailForBase64>

    <!-- title of the document -->
    <documentTitle>Extent Framework</documentTitle>

    <!-- report name - displayed at top-nav -->
    <reportName>Build 1</reportName>

    <!-- timestamp format -->
    <timeStampFormat>MMM dd, yyyy HH:mm:ss</timeStampFormat>

    <!-- custom javascript -->
    
  

    <!-- other configurations -->

    <!-- custom JavaScript -->
    <scripts>
      <![CDATA[
        $(document).ready(function() {
          // Add a class to the report container to apply the dark theme
          $('.extent').addClass('fancy-dark-theme');
        });
      ]]>
    </scripts>

    <!-- custom styles -->
    <styles>
      <![CDATA[
        /* Fancy dark theme styles */
        .fancy-dark-theme {
          background-color: #000000;
          color: #ffffff;
        }
        
        /* Modify other elements' styles to fit the fancy dark theme */
        /* For example:
        .report-name {
          color: #ff00ff;
          font-size: 24px;
        }
        .section-heading {
          color: #00ffff;
          font-weight: bold;
        }
        .test-name {
          color: #ffffff;
          font-style: italic;
        }
        .step-name {
          color: #ff00ff;
        }
        .step-details {
          color: #ffffff;
        }
        */

      ]]>
    </styles>

  </configuration>
</extentreports>
